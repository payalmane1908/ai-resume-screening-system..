version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - uploads:/app/uploads
    env_file:
      - .env
    environment:
      - FLASK_ENV=development
      - DATABASE_PATH=/app/instance/resume_ai.db
    restart: always

volumes:
  uploads: